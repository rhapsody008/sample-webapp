apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  labels:
    app: web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - name: http
              containerPort: 80
          resources: {}

---
apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    app: web
spec:
  selector: {}
  ports:
    - name: http
      port: 8081
      targetPort: 80
  type: ClusterIP

